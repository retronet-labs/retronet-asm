# Workspace di sviluppo locale (punta ai checkout sibling), non versionato.
go.work
go.work.sum

# Binario della CLI
/retronet-asm
/retronet-asm.exe

# ROM assemblate: artefatti di build (gli esempi versionati sono i .asm)
*.rom
